# v3.6.2
name: simple_calendar
description: My Calendar App v3.6.2
publish_to: none
version: 3.6.2+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  table_calendar: ^3.1.2
  flutter_secure_storage: ^9.0.0
  flutter_local_notifications: ^18.0.0
  timezone: ^0.9.4
  flutter_timezone: ^5.0.1
  file_picker: ^8.1.0
  permission_handler: ^11.3.0
  lunar: ^1.7.6
  path_provider: ^2.1.2
  share_plus: ^7.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true